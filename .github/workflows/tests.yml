name: 🧪 Tests del Cliente Web

on:
  pull_request:
    branches: [ main, develop ]
    types: [opened, synchronize, reopened]
  push:
    branches: [ main, develop ]

permissions:
  contents: read
  issues: write
  pull-requests: write
  actions: read
  checks: write

jobs:
  test-web-client:
    name: 🧪 Tests Cliente Web
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        node-version: ['20.x']
    
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'

      - name: Cache de dependencias Node
        uses: actions/cache@v3
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Instalar dependencias
        run: npm ci

      - name: Verificar instalación
        run: |
          node --version
          npm --version
          echo "✅ Node.js y npm configurados correctamente"

      - name: Ejecutar tests unitarios con cobertura
        run: npm run test:coverage
        continue-on-error: false

      - name: Verificar cobertura ≥80%
        run: |
          if [ -f coverage/coverage-summary.json ]; then
            COVERAGE=$(node -e "const c=require('./coverage/coverage-summary.json'); console.log(c.total.lines.pct)")
            echo "Cobertura actual: ${COVERAGE}%"
            if (( $(echo "$COVERAGE < 80" | bc -l) )); then
              echo "❌ Cobertura ${COVERAGE}% es menor al 80% requerido"
              exit 1
            fi
          fi

      - name: Ejecutar tests de integración
        run: npm run test:integration

      - name: Ejecutar tests de accesibilidad
        run: npm run test:accessibility || echo "⚠️ Tests de accesibilidad no disponibles aún"

      - name: Ejecutar tests de seguridad
        run: npm run test:security || echo "⚠️ Tests de seguridad no disponibles aún"

      - name: Subir reporte de cobertura
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: coverage-report
          path: coverage/
          retention-days: 30

      - name: Verificar build
        run: npm run build

  report-results:
    name: 📊 Reporte de Resultados
    runs-on: ubuntu-latest
    needs: [test-web-client]
    if: always() && github.event_name == 'pull_request'
    steps:
      - name: Comentar resultados en PR
        uses: actions/github-script@v6
        with:
          script: |
            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const botComment = comments.find(comment => 
              comment.user.login === 'github-actions[bot]' && 
              comment.body.includes('🧪 Resultados de Tests')
            );

            const webClientResult = '${{ needs.test-web-client.result }}';
            
            let body = `## 🧪 Resultados de Tests - Cliente Web\n\n`;
            
            if (webClientResult === 'success') {
              body += `### ✅ Estado: Exitoso\n\n`;
              body += `- **🌐 Cliente Web**: ✅ Todos los tests pasaron\n`;
              body += `- **📦 Build**: ✅ Build exitoso\n\n`;
              body += `🟢 **El PR está listo para revisión y merge.**\n`;
            } else if (webClientResult === 'failure') {
              body += `### ❌ Estado: Fallido\n\n`;
              body += `- **🌐 Cliente Web**: ❌ Tests fallaron o build falló\n\n`;
              body += `🔴 **El merge está bloqueado hasta que se corrijan los errores.**\n`;
            } else {
              body += `### ⏳ Estado: En progreso o cancelado\n\n`;
              body += `- **🌐 Cliente Web**: ⏳ ${webClientResult}\n`;
            }
            
            body += `\n---\n`;
            body += `*🤖 Este comentario se actualiza automáticamente en cada push*`;

            if (botComment) {
              github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: botComment.id,
                body: body
              });
            } else {
              github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: body
              });
            }

  test-e2e:
    name: 🎭 Tests E2E (Playwright)
    runs-on: ubuntu-latest
    needs: [test-web-client]
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Instalar Playwright browsers
        run: npx playwright install --with-deps

      - name: Ejecutar tests E2E
        run: npm run test:e2e
        env:
          PLAYWRIGHT_TEST_BASE_URL: http://localhost:3000

      - name: Subir reportes E2E
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 30

      - name: Subir videos E2E
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: playwright-videos
          path: test-results/
          retention-days: 7

  lighthouse-performance:
    name: ⚡ Lighthouse Performance
    runs-on: ubuntu-latest
    needs: [test-web-client]
    if: github.event_name == 'pull_request'
    steps:
      - name: Checkout código
        uses: actions/checkout@v4

      - name: Configurar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'
          cache: 'npm'

      - name: Instalar dependencias
        run: npm ci

      - name: Build de producción
        run: npm run build
        env:
          CI: true

      - name: Instalar Lighthouse CI
        run: npm install -g @lhci/cli@0.12.x

      - name: Ejecutar Lighthouse CI
        run: npm run lighthouse || echo "⚠️ Lighthouse CI requiere configuración adicional"
        continue-on-error: true

      - name: Subir reportes Lighthouse
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: lighthouse-reports
          path: lighthouse-reports/
          retention-days: 30

  check-overall-status:
    name: ✅ Estado General
    runs-on: ubuntu-latest
    needs: [test-web-client, test-e2e]
    if: always()
    steps:
      - name: Verificar estado general
        run: |
          echo "🔍 Verificando estado general de los tests..."
          
          WEB_CLIENT_RESULT="${{ needs.test-web-client.result }}"
          E2E_RESULT="${{ needs.test-e2e.result }}"
          
          # Verificar si hay fallos críticos
          if [[ "$WEB_CLIENT_RESULT" != "success" ]]; then
            echo "❌ Web Client tests failed or were cancelled"
            exit 1
          fi
          
          if [[ "$E2E_RESULT" != "success" && "$E2E_RESULT" != "skipped" ]]; then
            echo "⚠️ E2E tests failed, pero no bloquea el merge"
          fi
          
          echo "✅ Tests críticos pasaron exitosamente"

